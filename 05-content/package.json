{
  "name": "content",
  "private": true,
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "source": "start.ts",
  "scripts": {
    "debug": "parcel serve tests/index.html --public-url / --dist-dir dist",
    "build": "parcel build --no-optimize --no-source-maps --dist-dir ../dist/content",
    "dev": "nodemon -e js,ts,html,css,json,vue,ico,png --exec \"npm run build\""
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.8.3",
    "@parcel/transformer-sass": "^2.8.3",
    "@types/node": "^18.14.2",
    "chrome-types": "^0.1.171",
    "nodemon": "^2.0.20",
    "parcel": "^2.8.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
